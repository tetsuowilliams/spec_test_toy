---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---

# test_speckit Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-06-09

## Active Technologies
- Python 3.10+ (stdlib only; align with `pdf_count` and workspace rules) + None (standard library only) (002-count-pdf-directories)
- N/A (read-only directory traversal) (002-count-pdf-directories)
- N/A (read-only directory listing and stat) (003-print-file-sizes)

- Python 3.10+ (stdlib only; 3.10+ for consistent `Path`/`stat` usage patterns) + None (standard library only) (001-count-pdfs-in-directory)

## Project Structure

```text
src/
tests/
```

## Commands

cd src [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] pytest [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] ruff check .

## Code Style

Python 3.10+ (stdlib only; 3.10+ for consistent `Path`/`stat` usage patterns): Follow standard conventions

## Recent Changes
- 003-print-file-sizes: Added Python 3.10+ (stdlib only; align with `pdf_count` and workspace rules) + None (standard library only)
- 002-count-pdf-directories: Added Python 3.10+ (stdlib only; align with `pdf_count` and workspace rules) + None (standard library only)

- 001-count-pdfs-in-directory: Added Python 3.10+ (stdlib only; 3.10+ for consistent `Path`/`stat` usage patterns) + None (standard library only)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
